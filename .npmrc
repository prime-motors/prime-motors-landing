# Enforce engines.node from package.json
engine-strict=true
# Enforce pnpm usage + exact pnpm version (via packageManager)
package-manager-strict=true
package-manager-strict-version=true
# Make dependency problems fail early
strict-peer-dependencies=true
# Keep deps exact when you add new ones
save-exact=true
